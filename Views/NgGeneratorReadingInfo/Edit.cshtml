@model UtilityManagement.Models.TblNgGeneratorReadingInfo

@{
    ViewData["Title"] = "Edit NG Generator Reading";
}

<div class="container mt-4">

    <div class="row justify-content-center">
        <div class="col-md-10">

            <div class="card shadow-lg border-0">

                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0">Edit NG Generator Reading</h5>
                </div>

                <div class="card-body p-4">

                    <form asp-action="Edit" method="post">

                        <!-- Hidden ID -->
                        <input type="hidden" asp-for="Trid" />

                        <div asp-validation-summary="ModelOnly"
                             class="text-danger mb-3">
                        </div>

                        <div class="row g-3">

                            <!-- Date -->
                            <div class="col-md-3">
                                <label asp-for="Trdate" class="form-label">Date</label>
                                <input asp-for="Trdate" type="date" class="form-control" />
                            </div>

                            <!-- Equipment -->
                            <div class="col-md-3">
                                <label asp-for="Eqid" class="form-label">Equipment</label>

                                <select asp-for="Eqid"
                                        asp-items="ViewBag.EquipmentList"
                                        class="form-select">
                                    <option value="">-- Select Equipment --</option>
                                </select>
                            </div>

                            <!-- NG Consumption M -->
                            <div class="col-md-3">
                                <label asp-for="NgConsumptionM" class="form-label">NG Cons (m3)</label>
                                <input asp-for="NgConsumptionM" class="form-control" />
                            </div>

                            <!-- CNG Consumption -->
                            <div class="col-md-3">
                                <label asp-for="CngConsumption" class="form-label">CNG Cons (m3)</label>
                                <input asp-for="CngConsumption" class="form-control" />
                            </div>

                            <!-- E Generation -->
                            <div class="col-md-3">
                                <label asp-for="EGeneration" class="form-label">E Gen (Kwh)</label>
                                <input asp-for="EGeneration" class="form-control" />
                            </div>

                            <!-- Running Hr -->
                            <div class="col-md-3">
                                <label asp-for="RunningHr" class="form-label">Running Hr</label>
                                <input asp-for="RunningHr" class="form-control" />
                            </div>

                            <!-- NG / Hr -->
                            <div class="col-md-3">
                                <label asp-for="NgConsumptionHr" class="form-label">NG / Hr</label>
                                <input asp-for="NgConsumptionHr" class="form-control" />
                            </div>

                            <!-- NG / Kwh -->
                            <div class="col-md-3">
                                <label asp-for="NgConsumptionKwh" class="form-label">NG / Kwh</label>
                                <input asp-for="NgConsumptionKwh" class="form-control" />
                            </div>

                            <!-- Kwh per NG -->
                            <div class="col-md-3">
                                <label asp-for="KwhUnitNgConsumption" class="form-label">Kwh / NG</label>
                                <input asp-for="KwhUnitNgConsumption" class="form-control" />
                            </div>

                            <!-- Lub Oil -->
                            <div class="col-md-3">
                                <label asp-for="LubOilConsumption" class="form-label">Lub Oil</label>
                                <input asp-for="LubOilConsumption" class="form-control" />
                            </div>

                            <!-- Cost Fields -->
                            <div class="col-md-3">
                                <label asp-for="NgTkM" class="form-label">NG Tk/m3</label>
                                <input asp-for="NgTkM" class="form-control" />
                            </div>

                            <div class="col-md-3">
                                <label asp-for="ChemicalCost" class="form-label">Chemical Cost</label>
                                <input asp-for="ChemicalCost" class="form-control" />
                            </div>

                            <div class="col-md-3">
                                <label asp-for="LubOilCost" class="form-label">Lub Oil Cost</label>
                                <input asp-for="LubOilCost" class="form-control" />
                            </div>

                            <div class="col-md-3">
                                <label asp-for="CngCost" class="form-label">CNG Cost</label>
                                <input asp-for="CngCost" class="form-control" />
                            </div>

                            <!-- Others -->
                            <div class="col-md-3">
                                <label asp-for="Troubleshooting" class="form-label">Troubleshooting</label>
                                <input asp-for="Troubleshooting" class="form-control" />
                            </div>

                            <div class="col-md-3">
                                <label asp-for="ServiceCharge" class="form-label">Service Charge</label>
                                <input asp-for="ServiceCharge" class="form-control" />
                            </div>

                            <div class="col-md-3">
                                <label asp-for="RepairCharge" class="form-label">Repair Charge</label>
                                <input asp-for="RepairCharge" class="form-control" />
                            </div>

                            <!-- TOTAL -->
                            <div class="col-md-3">
                                <label asp-for="Total" class="form-label">Total</label>
                                <input asp-for="Total" class="form-control" readonly />
                            </div>

                            <!-- TK/KWH -->
                            <div class="col-md-3">
                                <label asp-for="TkKwh" class="form-label">Tk/Kwh</label>
                                <input asp-for="TkKwh" class="form-control" readonly />
                            </div>

                        </div>

                        <div class="d-flex justify-content-between mt-4">

                            <a asp-action="NgGeneratorReadingInfoList"
                               class="btn btn-outline-secondary">
                                ← Back
                            </a>

                            <button type="submit"
                                    class="btn btn-success px-4">
                                Update
                            </button>

                        </div>

                    </form>

                </div>

            </div>

        </div>
    </div>

</div>

<!-- ================= CALCULATION SCRIPT ================= -->
<script>
    document.addEventListener("DOMContentLoaded", function () {

        const fields = [
            "NgTkM",
            "ChemicalCost",
            "LubOilCost",
            "CngCost",
            "Troubleshooting",
            "ServiceCharge",
            "RepairCharge"
        ].map(id => document.getElementById(id));

        const total = document.getElementById("Total");
        const eGeneration = document.getElementById("EGeneration");
        const tkKwh = document.getElementById("TkKwh");

        function calculate() {

            let sum = 0;

            fields.forEach(f => {
                sum += parseFloat(f?.value || 0);
            });

            total.value = sum.toFixed(2);

            let gen = parseFloat(eGeneration?.value || 0);

            tkKwh.value = gen > 0
                ? (sum / gen).toFixed(2)
                : "0.00";
        }

        fields.forEach(f => {
            if (f) f.addEventListener("input", calculate);
        });

        eGeneration.addEventListener("input", calculate);

        calculate();
    });
</script>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}